{
  "name": "cypress_demo01",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "npx cypress run",
    "chromeTest": "npx cypress run --browser chrome --headed",
    "specfile": "npx cypress run --spec './cypress/e2e/frameworkImplementation.cy.js'"
    
  },
  "author": "Ritesh Ranjan",
  "license": "ISC",
  "devDependencies": {
    "@cypress/xpath": "^2.0.3",
    "cypress": "^12.7.0",
    "cypress-mochawesome-reporter": "^3.3.0"
  }
}
